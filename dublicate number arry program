#include <stdio.h>

int main()
{
    int a[]={5,2,2,4,5,4,6},i,j,len;
    len=sizeof(a)/sizeof(a[0]);
    printf("len=%d\n",len);
    for(i=0;i<len;i++)
    {
        for(j=i+1;j<len;j++)
        {
            if(a[i]==a[j])
            {
                printf("dublicate number is =%d\n",a[i]);
            }
        }
    }
    
    printf("Hello World");

    return 0;
}
